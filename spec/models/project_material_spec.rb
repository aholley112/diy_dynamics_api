# require 'rails_helper'

# RSpec.describe ProjectMaterial, type: :model do
#   it { should belong_to(:project) }
#   it { should belong_to(:material) }

#   # Testing validations
#   it { should validate_presence_of(:quantity_needed) }
#   it { should validate_numericality_of(:quantity_needed).is_greater_than(0) }

#   it { should validate_presence_of(:price) }
#   it { should validate_numericality_of(:price).is_greater_than_or_equal_to(0.0) }
# end